@model IEnumerable<SzereloCegApp.Models.Diagnosztika>

@{
    ViewBag.Title = "Diagnosztikák";
}

<h2>Diagnosztikák</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HibaNeve)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HibaNeve)
        </td>
        <td>                      
            @Html.ActionLink("✖", "Delete", new { id=item.ID },new { @class = "" })
        </td>
    </tr>
}

</table>
